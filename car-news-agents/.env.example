# このファイルを .env としてコピーし、値を設定してください。
#   cp .env.example .env
# .env はコミットしないこと（.gitignore で除外されています）。

# Anthropic API（分析・執筆・校正の各エージェントが利用）
ANTHROPIC_API_KEY=

# X (Twitter) API v2 Bearer Token（未設定時はSNS収集をスキップします）
X_BEARER_TOKEN=

# note.com ログイン情報（未設定時は投稿をスキップし、ローカル保存のみ行います）
NOTE_EMAIL=
NOTE_PASSWORD=
